import React from 'react';
import ReactDOM from 'react-dom';
import SettingsApp from './scripts/SettingsApp';

const root = ReactDOM.createRoot(document.querySelector("#cb-app-root-settings"))
root.render(<SettingsApp />)
